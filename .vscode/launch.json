{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run MasterKeyApp",
            "type": "debugpy",
            "request": "launch",
            "module": "uvicorn",
            "args": ["app.main:create_app", "--factory", "--reload"],
            "cwd": "${workspaceFolder}",
            "justMyCode": true
        }
    ]
}
